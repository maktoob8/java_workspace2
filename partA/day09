day09
배열 
	배열
		자바에서는 고정된 길이의 자료구조
		동일한형식과 의미를 갖는 데이터들을 같은 배열에 저장
		배열 요소에 접근(읽기와 쓰기)은 index가 필요.
		인덱스는 0번부터 시작하며 순서를 갖는 자료구조.
	배열과 반복문
		배열에 저장된 데이터로 동일한 처리 필요. 반복문 사용
	String 배열
		String 변수는 참조 타입이므로 String 배열 요소는 참조값을 저장
		참조하는 문자열 객체는 힙 영역에 저장
	Wrapper 클래스
		Integer,Double...원시타입과 관련된 기능을 갖는 클래스
		parseInt,parseDouble 등등의 메소드로 타입 변환
		
	참조 타입과 메소드
		메소드 인자의 타입에 따라 분류
			call by value :로 데이터 값을 전달. 메소드 인자가 원시 타입
			call by reference : 메소드 인자로 객체의 참조값을 전달. 메소드 인자가 배열 또는 클래스 타입.
		call by reference 예외 클래스 String 
			String 은 불변 (immutable) 객체
				문자열 객체의 내용을 바꾸지 않는다. 문자열 메소드 또는 연결
				연산으로 내용이 바뀌면 객체가 새로 생성된다.
				(문자열 리터럴로 String 선언할때) 동일한 문자열을 한번만 생성해서 상수풀에 저장하여 사용하는 방식으로 효율을 높이는 장점이있다.
				단,메소드 또는 연결 연산이 많아지면 새로운 문자열 객체가 계속 생성되는 단점도 있다
				hashcode 값은 문자열 내용으로 만드는 방식으로 변경되어 다른 클래스 객체의 hashcode와 다른 점이 있다.
				new 연산으로 문자열 내용과 상관없이 객체를 새로 생성할 수 있다.
				
	참고 : git사용
		1. 사용 프로그램 : github desktop 설치
		2. 원격 저장소 : github.com 에 계정 만들기
		3. github desktop 실행 - email 선택해서 로그인
		4. git 프로그램 소스코드를 관리하기 위한 목적
			a. 관리? 소스코드의 변화 과정을 추적
			b. 공유 : 팀 작업할때 협업 툴
		5. 내 컴퓨터의 폴더는 로컬 저장소(리포지토리)
		6. github에 저장하는 폴더는 원격 저장소
		
			로컬 저장소에서 원격 저장소로 보내기(push)하면 관리와 공유가 시작됩니다
			
			로컬 저장소만 사용할 때는 '나'의 코드만 관리
			
			원격 저장소에 여러 팀원이 함께 공유하고 push하는 과정은 매우 신중하게 해야하는 작업
			
			로컬 저장소를 만들면 .git 숨은 폴더가 생성됩니다.